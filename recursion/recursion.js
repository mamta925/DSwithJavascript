/**
 * Recursion: is a process (a function in our case) that call itself
 * process: invoke the same function with a different input until it reaches to base case
 * 
 * 1. Base case
 * 2. Different Input
 */  